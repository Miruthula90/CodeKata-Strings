let str = "abbac";
let palindrome = "";
let templength=0;

for (let i = 0; i <= str.length; i++) {
    palindrome=palindrome+str[i];
}

function checkpalindrome(val)
{
    if (val.split('')===val.split('').reverse())
    {
        
    }
}
malayalam